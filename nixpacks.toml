[phases.setup]
nixPkgs = ["python311", "postgresql"]

[phases.install]
cmds = ["cd backend && pip install -r requirements.txt"]

[phases.build]
cmds = ["cd backend && python init_db.py || true"]

[start]
cmd = "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
